import styled from "styled-components";

export const ProductList = styled.ul({
  display: "flex",
  flexWrap: "wrap",
  gap: "20px",
  margin: "20px",
  padding: 0,

  li: {
    listStyle: "none",
  },
});
